import 'package:flutter/cupertino.dart';

class DealItemWidget extends StatelessWidget {
  const DealItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
